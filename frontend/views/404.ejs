<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('partials/head'); %>
    <title>Página no encontrada - Sistema de Catálogo</title>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-custom fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fa-solid fa-store me-2"></i>
                <strong>Catálogo Digital</strong>
            </a>
            
            <div class="navbar-nav ms-auto">
                <% if (userName) { %>
                    <div class="d-flex align-items-center">
                        <div class="user-info me-3">
                            <i class="fa-solid fa-user"></i>
                            <span><%= userName %></span>
                        </div>
                        <a href="/logout" class="btn btn-logout">
                            <i class="fa-solid fa-right-from-bracket me-1"></i>
                            Cerrar sesión
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Contenido principal -->
    <div class="catalog-container" style="padding-top: 100px;">
        <div class="container">
            <div class="row">
                <div class="col-lg-6 mx-auto text-center">
                    <div class="error-page">
                        <i class="fa-solid fa-exclamation-triangle fa-5x text-warning mb-4"></i>
                        <h1 class="display-1 fw-bold text-muted">404</h1>
                        <h2 class="h3 mb-4">Página no encontrada</h2>
                        <p class="text-muted mb-5">
                            Lo sentimos, la página que buscas no existe o ha sido movida.
                        </p>
                        
                        <div class="d-grid gap-2 d-md-block">
                            <a href="/" class="btn btn-primary btn-lg me-md-2">
                                <i class="fa-solid fa-home me-2"></i>
                                Ir al Inicio
                            </a>
                            <a href="/about" class="btn btn-outline-secondary btn-lg">
                                <i class="fa-solid fa-info-circle me-2"></i>
                                Acerca de
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        .error-page {
            padding: 60px 20px;
        }
        
        .error-page i.fa-exclamation-triangle {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
    </style>
</body>
</html>
